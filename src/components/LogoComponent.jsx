import { Text } from "react-native";

const LogoComponent = () => {
  return (
    <Text
      style={{
        color: "#E0C85F",
        fontSize: 52,
        fontWeight: "600",
        letterSpacing: -4,
      }}
    >
      gbk
    </Text>
  );
};

export default LogoComponent;
